import {photoType} from './photoType'
import {setType} from './setType'
import {tagType} from './tagType'
import {albumType} from './albumType'

export const schemaTypes = [photoType, setType, tagType, albumType]
